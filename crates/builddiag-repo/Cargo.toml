[package]
name = "builddiag-repo"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Repository state loading for builddiag build contract validator"
repository = "https://github.com/user/builddiag"
homepage = "https://github.com/user/builddiag"
keywords = ["rust", "build", "validation"]
categories = ["development-tools"]

[dependencies]
anyhow.workspace = true
serde.workspace = true
toml.workspace = true
cargo_metadata.workspace = true
camino.workspace = true
builddiag-domain = { path = "../builddiag-domain" }
builddiag-types = { path = "../builddiag-types" }

[dev-dependencies]
tempfile.workspace = true
