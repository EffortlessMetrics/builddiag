# .github/settings.yml
repository:
  description: "Offline build-contract sensor for Rust workspaces (JSON receipts + PR-ready Markdown)"
  homepage: "https://github.com/EffortlessMetrics/builddiag"
  topics: rust, build-contract, workspace, cli, ci, policy, diagnostics, receipts, github-actions, cockpit
