# .github/settings.yml
repository:
  description: "CLI tool to validate the build contract of Rust repositories"
  homepage: "https://github.com/EffortlessMetrics/builddiag"
  topics: rust, cli, build, validation, msrv, devex, receipts, cockpit
